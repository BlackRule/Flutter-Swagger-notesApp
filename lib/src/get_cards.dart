import 'package:bloc_login/api_connection/api_connection.dart';
import 'package:bloc_login/model/api_model.dart';
import 'package:flutter/widgets.dart';

const url = "https://trello.backend.tests.nekidaem.ru/";

class GetCard {
  Future<List<Widget>> getCards(int row, Token token) async {
    // return getCards(row, token.token)
  }
}
